# TreeForLife Environment Configuration

# Database Connection (Optional)
# If left unset or empty, the app defaults to embedded PGlite under .data/pglite with zero dependencies.
# For cloud/production (e.g. Neon PostgreSQL), provide a Postgres connection URI:
# DATABASE_URL=postgres://user:password@ep-cool-forest-123456.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Application Settings
PORT=3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
